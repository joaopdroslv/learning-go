package main // Defines this as the main package, the starting point of the program

import (
	"fmt" // Imports the fmt package for formatted I/O operations
)

// Main function, the entry point of the program
func main() {  
	// Using the format package.function
	fmt.Println("Hello, World!")  // Prints a line of text to the console
}
